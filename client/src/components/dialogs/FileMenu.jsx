/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import { Menu } from "@mui/material";
import React from "react";

const FileMenu = ({ anchorEl = undefined }) => {
  return (
    <Menu open anchorEl={anchorEl}>
      abcs
    </Menu>
  );
};

export default FileMenu;
